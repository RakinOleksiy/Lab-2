# 📋 Менеджер Завдань - CI/CD Демонстраційний Проєкт

## 📖 Опис

Веб-застосунок для управління завданнями, створений для демонстрації налаштування серверу безперервної інтеграції (CI/CD) в рамках лабораторної роботи №2.

## 🎯 Функціональність

- ✅ Створення, перегляд, редагування та видалення завдань
- 📊 Статистика виконання завдань в реальному часі
- 🎨 Сучасний адаптивний інтерфейс
- 🔄 RESTful API
- 🧪 Покриття unit тестами
- 🐳 Docker підтримка
- 🚀 CI/CD pipeline з Jenkins та GitHub Actions

## 🛠 Технології

- **Backend**: Python 3.11, Flask
- **Testing**: pytest, unittest, coverage
- **Quality**: flake8, pylint, black
- **CI/CD**: Jenkins, GitHub Actions
- **Containerization**: Docker, Docker Compose
- **Reverse Proxy**: Nginx

## 📋 Вимоги

- Python 3.9+
- pip
- Docker та Docker Compose (для контейнеризації)
- Git

## 🚀 Швидкий старт

### Локальний запуск

```bash
# Клонування репозиторію
git clone https://github.com/your-username/task-manager.git
cd task-manager

# Створення віртуального середовища
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# або
venv\Scripts\activate  # Windows

# Встановлення залежностей
pip install -r requirements.txt

# Запуск додатку
python app.py
```

Додаток буде доступний за адресою: http://localhost:5000

### Запуск з Docker

```bash
# Збірка та запуск контейнера
docker build -t task-manager .
docker run -p 5000:5000 task-manager
```

### Запуск з Docker Compose

```bash
# Запуск всіх сервісів (додаток + Jenkins + Nginx)
docker-compose up -d

# Додаток: http://localhost
# Jenkins: http://localhost/jenkins
```

## 🧪 Тестування

```bash
# Запуск всіх тестів
pytest test_app.py -v

# Тести з покриттям коду
pytest test_app.py -v --cov=app --cov-report=html

# Лінтинг
flake8 app.py --max-line-length=120
pylint app.py

# Перевірка форматування
black --check app.py test_app.py
```

## 🔧 Налаштування Jenkins

### 1. Встановлення Jenkins

```bash
# Запуск Jenkins через Docker Compose
docker-compose up -d jenkins

# Отримання початкового пароля
docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
```

### 2. Налаштування проєкту

1. Відкрийте Jenkins: http://localhost:8080
2. Створіть новий Pipeline проєкт
3. У розділі "Pipeline" виберіть "Pipeline script from SCM"
4. Вкажіть URL вашого Git репозиторію
5. Вкажіть шлях до Jenkinsfile

### 3. Налаштування Webhooks

**GitHub:**
1. Settings → Webhooks → Add webhook
2. Payload URL: `http://your-jenkins-url/github-webhook/`
3. Content type: `application/json`
4. Events: "Just the push event"

**Bitbucket:**
1. Repository settings → Webhooks → Add webhook
2. URL: `http://your-jenkins-url/bitbucket-hook/`
3. Triggers: "Repository push"

## 📊 CI/CD Pipeline

### Jenkins Pipeline включає:

1. **Підготовка** - перевірка середовища
2. **Встановлення залежностей** - створення venv та встановлення пакетів
3. **Лінтинг коду** - flake8, pylint
4. **Форматування коду** - black
5. **Модульні тести** - pytest з покриттям
6. **Інтеграційні тести** - API тести
7. **Збірка додатку** - компіляція Python
8. **Генерація звітів** - HTML звіти покриття коду

### GitHub Actions включає:

- Матричні тести на Python 3.9, 3.10, 3.11
- Лінтинг та форматування
- Unit тести з покриттям
- Збірка Docker образу
- Сканування безпеки (Trivy)
- Автоматичне розгортання

## 📁 Структура проєкту

```
task-manager/
├── app.py                      # Основний додаток Flask
├── test_app.py                 # Unit тести
├── requirements.txt            # Python залежності
├── Jenkinsfile                 # Jenkins Pipeline
├── Dockerfile                  # Docker конфігурація
├── docker-compose.yml          # Docker Compose конфігурація
├── nginx.conf                  # Nginx конфігурація
├── .gitignore                  # Git ignore файл
├── .github/
│   └── workflows/
│       └── ci.yml              # GitHub Actions workflow
└── README.md                   # Документація
```

## 🔗 API Endpoints

- `GET /` - Головна сторінка
- `GET /health` - Перевірка стану додатку
- `GET /api/tasks` - Отримати всі завдання
- `GET /api/tasks/<id>` - Отримати завдання за ID
- `POST /api/tasks` - Створити нове завдання
- `PUT /api/tasks/<id>` - Оновити завдання
- `DELETE /api/tasks/<id>` - Видалити завдання
- `GET /api/stats` - Отримати статистику

## 📈 Покриття тестами

Проєкт має високе покриття тестами:
- Unit тести для TaskManager класу
- Інтеграційні тести для API endpoints
- Покриття коду > 90%

## 🤝 Внесок

Це навчальний проєкт для демонстрації CI/CD практик.

## 📄 Ліцензія

Цей проєкт створено для освітніх цілей.

## 👨‍🎓 Автор

Національний аерокосмічний університет ім. М. Є. Жуковського «ХАІ»


**Примітка**: Цей проєкт створено в рамках лабораторної роботи №2 з дисципліни "Інженерія програмного забезпечення".

